import { TextEmptyPipe } from './text-empty.pipe';

describe('TextEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new TextEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
