import { GetFormControlErrorDirective } from './get-form-control-error.directive';

describe('GetFormControlErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new GetFormControlErrorDirective();
    expect(directive).toBeTruthy();
  });
});
